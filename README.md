# Url Shortener

- Node.js
- Express.js

###### How to start the project:

- ```npm run start``` - The server is started on port 3000

###### Endpoints:

- `POST /new`
- `GET /url/:urlId`
- `DELETE /url/:urlId`
- `GET /:num`